// app/seller/signup/layout.tsx
// Bare layout for signup page — no sidebar, no auth check

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
